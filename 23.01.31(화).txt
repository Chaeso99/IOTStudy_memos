23.01.31(화)
<오전수업>
#로그인 #데이터베이스
종이책 받음<-복습할때랑 모르는거 찾아볼때 사용, 수업은 인터넷 교재로 진행

<데이터베이스의 종류:여기 적힌것 외에도 엄청 많음>
MySQL
Oracle 대기업에서 많이 사용, 연 유지비가 몇억에서 몇십억/오라클이 기술지원을 해주기 때문에 사용
MariaDB 작은 회사들이나 스타트업 대부분이 사용
SQL Server
SQLite 안드로이드나 IOS에서 사용하는 모바일 전용
Access

데이터베이스는 모두 SQL이라는 언어를 사용함, 기본문법은 같고 함수가 조금씩 다르다
데이터베이스는 엑셀과 비슷하다. 엑셀의 시트하나가 테이블, 엑셀파일하나가 스키마임

Oracle 데이터베이스/DBeaver 다운로드함(오라클설치비번:dita1234)
디비버 켜서 파일 바로아래 아이콘 누르고 오라클 클릭=>윈도우에서 run 검색해서 run sql 커맨드라인
=>connect system 입력=>password 입력(오라클 설치할때 입력했던 비번)=>Connected. 글자나옴
=>select 1 from dual; 입력=>1두개가-----으로 나눠진 상태로 나오면 정상=>커맨드라인 종료
=>디비버 커넥션 화면에서 localhost밑의 ORCL을 XE로 변경=>Password를 아까 입력했던 오라클
설치비번으로=>TestConnection누르기=>다운로드 다 된 후 Connected 나오면 정상=>확인, 완료
=>화면왼쪽 데이터베이스 네비게이터에서 localhost 눌러보면 Schemas 밑의 HR이 있음. 이걸
이용해서 실습할것임=>HR안의Tables확인

<데이터베이스 컬럼 타입 종류>
NUMBER:숫자 
DATE:날짜
CHAR:한글자
VARCHAR:가변스트링, 타입이 유동적
VARCHAR2:가변스트링(타입 고정, 대부분 이거 쓰면됨)
BLOB:바이너리(이미지, 영상, 음성)
CLOB:대용량 스트링
//보통 BLOB, CLOB 등은 따로 클라우드에 올려두고 주소만 데이터베이스에 넣음(데이터가 커서
느려질수 있음)
오라클에서는 BOOLEAN 대신에 CHAR 0/1 사용함. 0이 FALSE 1이 TRUE임

<기본 문법 예약어>적는순서는 위에서부터 적으면 되고 앞의 숫자는 작동되는 순서임./대문자로적기
5 SELECT [컬럼명]
1 FROM [테이블명]
2 WHERE [조건]
3 GROUP BY [그룹기준컬럼]
4 HAVING [그룹조건]
6 ORDER BY [정렬기준컬럼]

실무에서 쿼리 짤때는 * 찍으면 안됨
주석적는법: --주석내용
SELECT * FROM EMPLOYEES e; 에서 e는 별명임
문장 마지막에 꼭 ; 치기

예약어
파일 출력
distinct - jobid
문자 표현
숫자 표현
as 활용
연결연산자||

띄어쓰기 하려면 ""로 감싸줘야한다.
오라클 csv 한글 깨짐 등으로 검색하면 한글깨짐 고칠수있음
<오후수업>
if문이 있는 sql도 있고, if문이 없는 sql도 있다.

간단한거라도 나만의 서비스를 만들어보는게 좋음(아이디어 기획이라도)

보통 그룹함수는 개별적으로 짜면 안됨 그룹바이를 쓰고 그룹함수를 써야 정석임
보통 GROUP BY 한 컬럼은 SELECT에 적음

ANSI SQL이란?
미국 표준 협회에서 표준 SQL문을 정립 시켜 놓은 것. 대부분의 DBMS에 호환되므로 이 방식을
사용해야 편하다.

관계형 데이터베이스는 컬럼안에 객체를 저장할 수 없다. JOIN으로 연결함
(연관관계를 맺는다)

JOIN에서 ON을 쓰는게 좋은 이유:WHERE을 조건으로만 쓰는게 안 헷갈림