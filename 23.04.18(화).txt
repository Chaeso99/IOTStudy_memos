23.04.18(화)
[수업] 서블릿->JSP->JSP/서블릿중에서 JSP 단계가 model1 방식이고 서블릿에서 다 짜고 JSP에서 화면만 뿌리는게 model2 방식이다. 우리조 프로젝트 진행방식은 model1, 우리가 지금 배울건 model2

JSP에 로직을 넣어도 상관은 없지만 나중에 가면 유지보수가 너무 복잡해져서 현업에서는 이렇게 사용하지 않음.

Servlet에서 request, response 관련 기능들을 모두 처리하고 jsp로 데이터만 넘겨주면 됨

<login-practice 프로젝트>
일반페이지
로그인페이지
로그인 성공페이지

일반페이지->로그인페이지로 이동->form 데이터로 입력해서 로그인 요청->로그인 성공 페이지로 이동

프로젝트의 WEB-INF 밑의 lib폴더에 Tomcat9.0의 lib에 있는 jsp-api.jar와 servlet-api.jar 복사해서 넣어주면 됨 => 넣어주고 나면 Libraries밑에 WebAppLibraries가 나오고 그밑에 우리가 넣은애들이 보인다.

redirect ->새로운 요청
ㄴjsp에서 전부 처리해야하는데 원래의 데이터가 없어서 문제가 발생함

foward ->기존 요청 승계
ㄴservlet에서 모든 로직 처리, jsp에서 servlet에서 넘겨준 데이터 출력만 하면 됨

WEB-INF/views에 넣는 이유:webapp밑에 폴더 만들어서 넣어도 되는데 그렇게하면 외부인들이 모두 볼 수 있어서. WEB-INF 안에 있는 자료는 외부인들이 볼 수 없다.

어트리뷰트를 리퀘스트에 넣고 리퀘스트를 리퀘스트디스패처에 넣고 리퀘스트 디스패처.forward함

formdata를 객체로 만드는 방법 2가지
-모든 객체에 id를 준다.

$.ajax
ㄴfetch/ajax ->비동기 통신(페이지를 이동(새로고침 포함)하지 않고 데이터를 주고 받을 때)에 사용한다. 관련해서 TCP스쿨 사이트에서 AJAX/JQUERY 공부해야함

ex)회원가입/로그인/글작성/댓글작성/좋아요 등등 페이지 이동이 아닌 대부분이 비동기 통신 방식으로 구현해야함

위까지는 데이터를 객체로 보냈음
<데이터를 폼데이터로 보내기>